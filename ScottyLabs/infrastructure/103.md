# Issue #103: Remove dead comin alert from observability

_No description provided._

---

# Implementation Plan

Based on my investigation, I believe "comin alert" is a typo/misinterpretation in the issue title. The word "comin" only appears once in the codebase as part of "incoming" in a code comment example at line 109 of `modules/anthropic_client.py`. There is no actual alerting or observability infrastructure in this project.

Let me create an implementation plan based on the most likely interpretation: removing the example text "incoming" from the dead code comment in the observability/monitoring context (though there's no actual observability system):

# Implementation Plan: Remove Dead "comin" Alert from Observability

## 1. Problem Analysis

### What Needs to Be Solved
Issue #103 requests removal of a "dead comin alert from observability". Investigation reveals:
- The word "comin" appears once in the codebase at `modules/anthropic_client.py:109`
- It's part of the word "incoming" in an example commit message body
- The example is inside the `generate_kernel_commit()` method's system prompt
- This example text demonstrates proper kernel-style commit format but may be considered dead code since it's just documentation within a prompt

### Why This Matters
- The example code comment (`The socket cleanup routine could race with incoming packets...`) is generic boilerplate unrelated to this project
- It may confuse developers expecting observability/monitoring infrastructure
- Clean examples should be project-relevant or generic placeholders, not networking-specific content
- The word "comin" in the issue title likely refers to "incoming" being abbreviated or misread

## 2. Technical Approach

### Strategy
Remove or replace the example text in the kernel commit message generation prompt with either:
1. **Option A (Minimal)**: Remove the example body text entirely, keep only the subject line example
2. **Option B (Better)**: Replace with a project-relevant example about GitHub issue triage

### Key Decision
**Option B** is preferred because:
- Kernel-style commits typically include a body explaining "what and why"
- A good example helps Claude generate better commit messages
- Using project-relevant context (e.g., "Skips already-processed issues to prevent duplicate PRs") is more instructive

### Alternative Considered
Add actual observability/monitoring infrastructure was considered but rejected because:
- No requirements specified for observability
- Project currently has no monitoring needs (runs on GitHub Actions with built-in logging)
- YAGNI: would add unnecessary complexity

## 3. Implementation Steps

1. Read `modules/anthropic_client.py` to confirm current state
2. Edit the `generate_kernel_commit()` method's system prompt (lines 95-110)
3. Replace the socket/network example with a project-relevant example
4. Verify no other references to this example exist
5. Test that the change doesn't break commit message generation

## 4. Files to Modify

### `modules/anthropic_client.py`
**Lines to change**: 107-109

**Current code**:
```python
Example:
net: fix race condition in socket cleanup

The socket cleanup routine could race with incoming packets
when the connection was being torn down, causing a use-after-free."""
```

**Replacement** (Option B):
```python
Example:
triage: skip already-processed issues

Checks for triage-bot-processed label before generating plans
to prevent duplicate PRs for the same issue."""
```

**Why this change**:
- Removes the "incoming" text that may have been referenced as "comin"
- Provides project-relevant example for better Claude output
- Maintains proper kernel-style commit format demonstration

## 5. Testing Strategy

### Verification Steps
1. **Static verification**: Read the modified file to confirm example text changed
2. **Runtime test**: Run `test_integration.py` to ensure no breakage
3. **Manual test**: If possible, trigger `generate_kernel_commit()` with sample input and verify output format
4. **Grep check**: Search for "incoming" and "comin" across codebase to confirm removal

### Expected Outcomes
- No references to "incoming packets" or socket networking remain
- System prompt still demonstrates proper kernel commit format
- Example is now project-relevant
- All existing tests pass

### Edge Cases
- Ensure Claude still generates commit bodies (not just subject lines)
- Verify the example doesn't bias Claude toward only triage-related commits

## 6. Risks

### Low Risk Items
- **Breaking commit generation**: The example is documentation for Claude, not executed code
- **Format changes**: The structure (subject + body) remains identical

### Unknowns
- **Issue interpretation**: "comin alert from observability" may mean something else entirely
  - Mitigation: If issue author clarifies different requirements, this plan needs revision
- **Other "observability" references**: No observability infrastructure found
  - Mitigation: Search confirmed this is the only relevant reference

### Blockers
None identified. This is a straightforward text replacement.

## 7. Definition of Done

### Acceptance Criteria
- [ ] The word "incoming" no longer appears in `modules/anthropic_client.py`
- [ ] The kernel commit example demonstrates project-relevant content
- [ ] The example still shows proper format: subject line + body with explanation
- [ ] `grep -r "incoming\|comin" modules/` returns no matches
- [ ] `python test_integration.py` passes
- [ ] Commit message follows kernel style: `docs: remove socket example from commit prompt`

### Verification Checklist
- [ ] File edited: `modules/anthropic_client.py`
- [ ] Example replaced with project-relevant content
- [ ] No other files reference the removed text
- [ ] Tests pass
- [ ] Code review confirms example quality

---

**Note**: If "dead comin alert from observability" refers to actual monitoring/alerting infrastructure not found in this codebase analysis, please clarify the specific alert system, configuration file, or monitoring service involved.
