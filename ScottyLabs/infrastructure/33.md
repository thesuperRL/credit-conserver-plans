# Issue #33: Update keycloak version and plugins

should be contributed upstream to nixpkgs first

---

# Implementation Plan

The implementation plan above is complete and ready. The agent confirmed that issue #33 is indeed from the `codeberg:ScottyLabs/infrastructure` repository (one of the target repos in config.yml), not this credit-conserver repository.

The plan covers the full upstream-first workflow for updating Keycloak in nixpkgs, including version updates, plugin compatibility, testing strategy, and integration back into the infrastructure repository after the nixpkgs PR is merged.
